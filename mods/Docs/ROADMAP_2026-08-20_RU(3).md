# Cheat Menu Pro — Roadmap v7 / Fleet Composer PASS → Exact Ship Control

**Дата актуализации:** 22.08.2026  
**Статус:** beta17 Final RELEASED; beta18-pre2.4 Fleet Target Core RUNTIME PASS; beta18-pre3 Fleet Composer 2.0 RUNTIME PASS; beta18-pre4 current candidate.

## 1. Закрытые Navy gates

### beta18-pre2.4 — Fleet Scope Proof

- `MilitaryFormation` row scope доказан в runtime;
- native fleet panel bridge открывает exact Fleet;
- legacy CMP fleet marker/resolver не является основным foundation.

### beta18-pre3 — Fleet Composer 2.0

Пользователь подтвердил работу всех функций:

- пять произвольных Hull rows;
- duplicate hull rows;
- одна mixed fleet formation;
- vanilla + Tech & Res hulls;
- technology/provider gates;
- composer считается Runtime PASS и замораживается как creation baseline.

## 2. beta18-pre4 — Exact Ship Control & Flagship

Текущий gate:

- новый subtab `Корабли`;
- exact Ship selector поверх выбранного native Fleet;
- 100 позиций / 5 страниц;
- `ordered_scope_ship`, non-destroyed filter, ordering by `power_projection`;
- marker хранится непосредственно на exact `ship`;
- Ship Type diagnostics;
- flagship / damage / port / battle diagnostics;
- HP/Crew bands;
- exact `set_as_flagship = yes/no`;
- lost-target handling без silent fallback.

Runtime должен доказать отдельно разные Ships одного Ship Type и точность flagship write.

## 3. beta18-pre5 — Transfers / Retrofit / Amphibious / Supply

Только после PASS exact Ship:

- source Fleet + exact Ship(s) + receiver Country;
- `set_ship_owner` / `set_ship_owner_multiple`;
- `clear_ownership_transfer_fleet` после batch transfer;
- post-check source/receiver;
- native retrofit/template bridge без fake direct template write;
- amphibious assistant/checklist;
- national Supply Ship pool semantics отдельно от Fleet composition.

## 4. beta18-RC1 / Final

- Composer 2.0 regression;
- existing exact Fleet;
- exact Ship;
- flagship;
- single/batch transfer;
- port / sea / battle / damaged / destroyed;
- immediate / +1 day / save-load;
- Workspace 90/100/115/130;
- no hidden fallback / no ghost target.

## 5. После beta18

**Army Rework 2.0 → beta19 Technology 2.0 → beta20 Special & Quick → Economy/Markets/Regions Rework → Vanilla CMP Rework.**

## Release discipline

Registry/codegen source of truth; deterministic `--check`; zero missing GUI→SGUI/effect refs; zero duplicate `cmp_*`; RU/EN parity; no guessed scripting surface; static QA + runtime evidence before promotion.
