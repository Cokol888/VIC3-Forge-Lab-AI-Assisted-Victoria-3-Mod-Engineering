# Population & Society 2.0 in Workspace — beta15.5

beta15.5 migrates the existing Population 2.0 operations into the shared Workspace. Scripted effects, modifiers and ScriptedGui contracts remain unchanged; the page reuses the beta15.4 gameplay endpoints.

The page provides four content-only scrolling tabs — Population, Professions, Welfare and Society — in each 90/100/115/130 profile. The state target, navigation and fixed result footer remain visible. Long explanations live in composite hover tooltips and five persistent scrollable help cards per profile.

All 59 unique selection and action endpoints are covered by deterministic four-profile parity checks. The legacy Population panel remains in the package as an emergency fallback.
